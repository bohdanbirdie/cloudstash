// Stub for @react-email/code-block to avoid prismjs in Cloudflare Workers
// This is never actually used - just satisfies the optional peer dep
export const CodeBlock = () => null;
export default CodeBlock;
