/**
 * Shared typography utilities (Tailwind class strings).
 *
 * Compose with `cn(...)` from `@/lib/utils` when adding color/background.
 */

/**
 * Compact uppercase eyebrow label (10px). Use for tertiary/repeating labels
 * like capability badges, divider captions, or admin row labels.
 */
export const MICRO_LABEL =
  "text-[10px] font-semibold uppercase tracking-[0.08em] leading-none";

/**
 * Slightly larger uppercase eyebrow (11px). Use for the dominant label in a
 * tile header (e.g. plan name above the price).
 */
export const MICRO_LABEL_SM =
  "text-[11px] font-semibold uppercase tracking-[0.08em]";
