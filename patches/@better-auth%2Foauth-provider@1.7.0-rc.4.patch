diff --git a/dist/register-BotzQoS8.mjs b/dist/register-BotzQoS8.mjs
index 7b650bc..f94986c 100644
--- a/dist/register-BotzQoS8.mjs
+++ b/dist/register-BotzQoS8.mjs
@@ -501,0 +502,12 @@
+const OAUTH_RESOURCE_IDENTIFIER_CONSTRAINT = /UNIQUE constraint failed: oauth_resource\.identifier(?:\b|$)/i;
+function hasCauseMessage(error, pattern) {
+	let current = error;
+	const seen = /* @__PURE__ */ new Set();
+	while (current != null && !seen.has(current)) {
+		seen.add(current);
+		const message = current instanceof Error ? current.message : typeof current === "string" ? current : "";
+		if (pattern.test(message)) return true;
+		current = typeof current === "object" && "cause" in current ? current.cause : void 0;
+	}
+	return false;
+}
@@ -592 +604 @@
-				if (/unique|duplicate|UNIQUE/i.test(message)) {
+				if (/unique|duplicate|UNIQUE/i.test(message) || hasCauseMessage(err, OAUTH_RESOURCE_IDENTIFIER_CONSTRAINT)) {
@@ -1193,0 +1206 @@
+	const defaultApplicationType = input.registrationSource === "clientMetadataDocument" ? null : input.registrationSource === "dynamic" ? opts.clientRegistrationDefaultApplicationType ?? "web" : "web";
@@ -1197 +1210 @@
-	} : input.metadata, input.registrationSource === "clientMetadataDocument" ? null : "web");
+	} : input.metadata, defaultApplicationType);
diff --git a/dist/oauth-Bi2PA_d1.d.mts b/dist/oauth-Bi2PA_d1.d.mts
index 7bfd06f..a5c3231 100644
--- a/dist/oauth-Bi2PA_d1.d.mts
+++ b/dist/oauth-Bi2PA_d1.d.mts
@@ -1301,0 +1302,8 @@
+  /**
+   * Application type applied to dynamic registrations that omit
+   * `application_type`. Explicit client metadata is always preserved and
+   * validated normally.
+   *
+   * @default "web"
+   */
+  clientRegistrationDefaultApplicationType?: "web" | "native";
