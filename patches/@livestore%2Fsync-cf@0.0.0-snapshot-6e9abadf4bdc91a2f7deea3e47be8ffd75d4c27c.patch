diff --git a/node_modules/@livestore/sync-cf/.bun-tag-b6e1852b1bb2e4ce b/.bun-tag-b6e1852b1bb2e4ce
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/dist/cf-worker/do/layer.js b/dist/cf-worker/do/layer.js
index 4bb070a097498fc0fc1264ff74c4521feb5e0c77..659682b8f08d21d538713774e34b5c199ef1eaba 100644
--- a/dist/cf-worker/do/layer.js
+++ b/dist/cf-worker/do/layer.js
@@ -3,7 +3,7 @@ import { EventSequenceNumber, State } from '@livestore/common/schema';
 import { shouldNeverHappen } from '@livestore/utils';
 import { Effect, Predicate } from '@livestore/utils/effect';
 import { nanoid } from '@livestore/utils/nanoid';
-import { contextTable, eventlogTable } from "./sqlite.js";
+import { contextTable, eventlogTable, rpcSubscriptionTable } from "./sqlite.js";
 import { makeStorage } from "./sync-storage.js";
 const CacheSymbol = Symbol('Cache');
 export class DoCtx extends Effect.Service()('DoCtx', {
@@ -53,6 +53,11 @@ export class DoCtx extends Effect.Service()('DoCtx', {
             const colSpec = State.SQLite.makeColumnSpec(contextTable.sqliteDef.ast);
             doSelf.ctx.storage.sql.exec(`CREATE TABLE IF NOT EXISTS "${contextTable.sqliteDef.name}" (${colSpec}) strict`);
         }
+        {
+            // CLOUDSTASH PATCH (#1328 DO-RPC follow-up): durable rpc-subscription table, lazily created.
+            const colSpec = State.SQLite.makeColumnSpec(rpcSubscriptionTable.sqliteDef.ast);
+            doSelf.ctx.storage.sql.exec(`CREATE TABLE IF NOT EXISTS "${rpcSubscriptionTable.sqliteDef.name}" (${colSpec}) strict`);
+        }
         const storageRow = doSelf.ctx.storage.sql
             .exec(`SELECT * FROM "${contextTable.sqliteDef.name}" WHERE storeId = ?`, storeId)
             .toArray()[0];
@@ -67,7 +72,7 @@ export class DoCtx extends Effect.Service()('DoCtx', {
             // @ts-expect-error
             doSelf[CacheSymbol].currentHeadRef = { current: currentHead };
         };
-        const rpcSubscriptions = new Map();
+        const rpcSubscriptions = makeRpcSubscriptions(doSelf.ctx);
         const storageCache = {
             storeId,
             backendId,
@@ -88,4 +93,23 @@ export class DoCtx extends Effect.Service()('DoCtx', {
     }, UnknownError.mapToUnknownError, Effect.withSpan('@livestore/sync-cf:durable-object:makeDoCtx')),
 }) {
 }
+// CLOUDSTASH PATCH (#1328 DO-RPC follow-up): SQLite-backed rpc-subscription registry (replaces the
+// in-memory Map that was wiped on DO eviction). Keyed per caller DO id; survives hibernation.
+const makeRpcSubscriptions = (ctx) => {
+    const table = rpcSubscriptionTable.sqliteDef.name;
+    return {
+        set: (subscription) => ctx.storage.sql.exec(`INSERT OR REPLACE INTO "${table}" (durableObjectId, bindingName, requestId, storeId, payload, subscribedAt) VALUES (?, ?, ?, ?, ?, ?)`, subscription.callerContext.durableObjectId, subscription.callerContext.bindingName, subscription.requestId, subscription.storeId, subscription.payload === undefined ? null : JSON.stringify(subscription.payload), subscription.subscribedAt),
+        all: () => {
+            const rows = ctx.storage.sql.exec(`SELECT * FROM "${table}"`).toArray();
+            return rows.map((row) => ({
+                storeId: row.storeId,
+                requestId: row.requestId,
+                subscribedAt: row.subscribedAt,
+                callerContext: { bindingName: row.bindingName, durableObjectId: row.durableObjectId },
+                ...(row.payload !== null ? { payload: JSON.parse(row.payload) } : {}),
+            }));
+        },
+        remove: (durableObjectId) => ctx.storage.sql.exec(`DELETE FROM "${table}" WHERE durableObjectId = ?`, durableObjectId),
+    };
+};
 //# sourceMappingURL=layer.js.map
\ No newline at end of file
diff --git a/dist/cf-worker/do/push.js b/dist/cf-worker/do/push.js
index fcfa7c993b1fcf8f89f78a60b11ecea0a6b6f8d7..3bd62c7d6ddd0eb19f2a159c08e64223517c10e5 100644
--- a/dist/cf-worker/do/push.js
+++ b/dist/cf-worker/do/push.js
@@ -1,7 +1,7 @@
 import { BackendIdMismatchError, ServerAheadError, SyncBackend, UnknownError, } from '@livestore/common';
 import { emitStreamResponse } from '@livestore/common-cf';
 import { splitChunkBySize } from '@livestore/common/sync';
-import { Chunk, Effect, Option, Schema } from '@livestore/utils/effect';
+import { Chunk, Effect, Exit, Option, Schema } from '@livestore/utils/effect';
 import { MAX_PUSH_EVENTS_PER_REQUEST, MAX_WS_MESSAGE_BYTES } from "../../common/constants.js";
 import { SyncMessage } from "../../common/mod.js";
 import { WebSocketAttachmentSchema, } from "../shared.js";
@@ -102,18 +102,30 @@ export const makePush = ({ payload, headers, options, storeId, ctx, env, }) => (
             yield* Effect.logDebug(`Broadcasted to ${connectedClients.length} WebSocket clients`);
         }
         // RPC broadcasting would require reconstructing client stubs from clientIds
-        if (rpcSubscriptions.size > 0) {
-            for (const subscription of rpcSubscriptions.values()) {
+        // CLOUDSTASH PATCH (#1328 DO-RPC follow-up): read subscribers from durable SQLite (survives
+        // eviction) and prune any that fail delivery (the in-memory Map could never do this).
+        const subscriptions = rpcSubscriptions.all();
+        if (subscriptions.length > 0) {
+            for (const subscription of subscriptions) {
+                let delivered = true;
                 for (const { encoded } of responses) {
-                    yield* emitStreamResponse({
+                    const exit = yield* emitStreamResponse({
                         callerContext: subscription.callerContext,
                         env,
                         requestId: subscription.requestId,
                         values: [encoded],
                     }).pipe(Effect.tapCauseLogPretty, Effect.exit);
+                    if (Exit.isFailure(exit)) {
+                        delivered = false;
+                        break;
+                    }
+                }
+                if (delivered === false) {
+                    rpcSubscriptions.remove(subscription.callerContext.durableObjectId);
+                    yield* Effect.logDebug(`Dropped unreachable DO-RPC subscription ${subscription.callerContext.durableObjectId}`);
                 }
             }
-            yield* Effect.logDebug(`Broadcasted to ${rpcSubscriptions.size} RPC clients`);
+            yield* Effect.logDebug(`Broadcasted to ${subscriptions.length} RPC clients`);
         }
     }).pipe(Effect.tapCauseLogPretty, Effect.withSpan('push-rpc-broadcast'), Effect.uninterruptible, // We need to make sure Effect RPC doesn't interrupt this fiber
     Effect.fork);
diff --git a/dist/cf-worker/do/sqlite.js b/dist/cf-worker/do/sqlite.js
index b85bad71515b26bba592cb69936eadb5ce2cb9b5..d6724cc7adf0e21c9525f7ec57ea52d7afea2426 100644
--- a/dist/cf-worker/do/sqlite.js
+++ b/dist/cf-worker/do/sqlite.js
@@ -33,4 +33,20 @@ export const contextTable = State.SQLite.table({
         backendId: State.SQLite.text({}),
     },
 });
+/**
+ * CLOUDSTASH PATCH (livestorejs/livestore#1328 DO-RPC follow-up): durable registry of DO-to-DO RPC
+ * live-pull subscribers, so they survive sync-DO hibernation/eviction (the durable analogue of what
+ * ctx.getWebSockets() gives the WS path). Keyed per caller DO id.
+ */
+export const rpcSubscriptionTable = State.SQLite.table({
+    name: `rpc_subscription_${PERSISTENCE_FORMAT_VERSION}`,
+    columns: {
+        durableObjectId: State.SQLite.text({ primaryKey: true }),
+        bindingName: State.SQLite.text({}),
+        requestId: State.SQLite.text({}),
+        storeId: State.SQLite.text({}),
+        payload: State.SQLite.text({ nullable: true }),
+        subscribedAt: State.SQLite.integer({}),
+    },
+});
 //# sourceMappingURL=sqlite.js.map
\ No newline at end of file
diff --git a/dist/cf-worker/do/transport/do-rpc-server.js b/dist/cf-worker/do/transport/do-rpc-server.js
index 786ad13c3fc2c909187965300174f1dd9f606a21..7be0967103da356a7937d58882dedda4ebfd6be8 100644
--- a/dist/cf-worker/do/transport/do-rpc-server.js
+++ b/dist/cf-worker/do/transport/do-rpc-server.js
@@ -18,7 +18,7 @@ export const createDoRpcHandler = (options) => Effect.gen(this, function* () {
             const { rpcSubscriptions } = yield* DoCtx;
             // TODO rename `req.rpcContext` to something more appropriate
             if (req.rpcContext !== undefined) {
-                rpcSubscriptions.set(req.storeId, {
+                rpcSubscriptions.set({
                     storeId: req.storeId,
                     subscribedAt: Date.now(),
                     requestId: Headers.get(headers, 'x-rpc-request-id').pipe(Option.getOrThrow),
diff --git a/dist/cf-worker/do/transport/ws-rpc-server.js b/dist/cf-worker/do/transport/ws-rpc-server.js
index 169ea06c9ad4bf28d01e2ea289fa32b0837caf03..b10319b25d827a036c8b9cfdf7bef1e1928954f7 100644
--- a/dist/cf-worker/do/transport/ws-rpc-server.js
+++ b/dist/cf-worker/do/transport/ws-rpc-server.js
@@ -12,7 +12,10 @@ export const makeRpcServer = ({ doSelf, doOptions }) => {
             const headers = yield* getForwardedHeaders;
             return makeEndingPullStream({ req, payload: req.payload, headers }).pipe(
             // Needed to keep the stream alive on the client side for phase 2 (i.e. not send the `Exit` stream RPC message)
-            req.live === true ? Stream.concat(Stream.never) : identity, Stream.provideLayer(DoCtx.Default({ doSelf, doOptions, from: { storeId: req.storeId } })), Stream.mapError((cause) => cause._tag === 'UnknownError' || cause._tag === 'BackendIdMismatchError'
+            // CLOUDSTASH PATCH (hibernation, livestorejs/livestore#1328): timer-less live-pull hold —
+            // Stream.never parks on Effect.never (a setInterval that blocks DO hibernation); the
+            // timer-less Effect.async(() => {}) holds the stream open identically without a timer.
+            req.live === true ? Stream.concat(Stream.fromEffect(Effect.async(() => {}))) : identity, Stream.provideLayer(DoCtx.Default({ doSelf, doOptions, from: { storeId: req.storeId } })), Stream.mapError((cause) => cause._tag === 'UnknownError' || cause._tag === 'BackendIdMismatchError'
                 ? cause
                 : new UnknownError({ cause })));
         }).pipe(Stream.unwrap),
