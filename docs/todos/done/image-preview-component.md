# Image preview component

Skeleton loading state with delayed fade-in (200ms), unified `ImageOff` fallback for failed / missing images. Replaces inline image+monogram blocks in list rows and detail hero.
